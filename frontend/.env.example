# Base URL of the backend API (without trailing slash)
VITE_API_URL=http://localhost:8000/api/v1

# While the backend is in development, set to "true" to use bundled mock data.
# Set to "false" to talk to the real API defined by VITE_API_URL.
VITE_USE_MOCKS=true
